# filerate

Simple C program to read / monitor the file size of files. This was useful for another project I'm working on. 


## Build
Just run `make`. Optionally change the compiler to your compiler of choice.